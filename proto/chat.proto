syntax = "proto3";
package chat;

option go_package = '/chat';

message Message {
    string body = 1;
    int64 messageNumber = 2;
    int64 timestamps = 3; 
}

service ChatService {
    rpc SayHello(Message) returns (Message) {}
}